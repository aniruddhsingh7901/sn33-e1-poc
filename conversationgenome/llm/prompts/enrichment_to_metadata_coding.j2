Analyze the following coding-related enrichment content (typically a search-result title and snippet) and extract technical topic tags.

Generate 8 to 12 short technical topic tags in lowercase that a semantic embedding model would consider close in meaning to the core technical theme.

Tag rules:
- Lowercase only
- 1 to 3 words each
- No punctuation, no hyphens, no underscores (write `rest api` not `rest-api`)
- No duplicates
- Common English / common technical vocabulary only — well-known framework or language names are fine; avoid invented compound words, jargon-only abbreviations, and typos
- No source name, no author name, no greetings or filler

Return ONLY a comma-delimited list of tags with no commentary.

Enrichment content:
{{ enrichment_content }}
