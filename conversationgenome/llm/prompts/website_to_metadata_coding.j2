Analyze the following coding-related webpage content and extract technical topic tags. Focus on the core technical subject matter; ignore navigation menus, ads, and boilerplate.

Generate 12 to 15 short technical topic tags in lowercase that a semantic embedding model would consider close in meaning to the page's core technical theme.

Tag rules:
- Lowercase only
- 1 to 3 words each
- No punctuation, no hyphens, no underscores (write `rest api` not `rest-api`)
- No duplicates
- Common English / common technical vocabulary only — well-known framework or language names like `python`, `react`, `typescript`, `docker`, `pytorch`, `postgres` are fine; avoid invented compound words, jargon-only abbreviations, and typos
- No site name, no author name, no greetings or filler

Return ONLY a comma-delimited list of tags with no commentary.

Webpage content:
{{ website_content }}
