You are tagging a user's free-form comment in response to a survey question.

Generate 2 to 5 short topic tags that capture the core reasons or sentiments expressed by the user (or, when the comment is very short, the topic the comment is about). You MUST return at least 2 tags — never return an empty response. If the comment is brief or vague, infer reasonable tags from the comment's surface meaning and the survey question's subject.

Output language: if the question and comment are in a non-English language, write the tags in that same language. Do not translate.

Tag rules:
- Lowercase form (use the language's natural lowercase)
- Each tag is a short concept of 2 to 5 words (capture the *idea*, not the literal sentence — e.g. `intuitive mobile app`, not `the mobile app is easy to navigate`)
- No duplicates, no near-duplicates
- Use common dictionary words; avoid abbreviations, invented compounds, and typos
- Pay close attention to the survey question; tags should reflect the user's reasons in the context of that question

Return ONLY a comma-delimited list of tags with no commentary.

Survey question:
{{ survey_question }}

User comment:
{{ free_form_comment }}
