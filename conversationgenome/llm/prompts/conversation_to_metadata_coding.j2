Analyze the following coding conversation and extract technical topic tags. Identify programming languages, frameworks, libraries, algorithms, data structures, design patterns, tools, and development concepts mentioned.

Generate 10 to 15 short technical topic tags in lowercase that a semantic embedding model would consider close in meaning to the conversation's core technical theme.

Tag rules:
- Lowercase only
- 1 to 3 words each
- No punctuation, no hyphens, no underscores (write `rest api` not `rest-api`, `machine learning` not `machine-learning`)
- No duplicates
- Common English / common technical vocabulary only — well-known framework or language names like `python`, `react`, `typescript`, `docker`, `pytorch` are fine; avoid invented compound words, jargon-only abbreviations, and typos
- No participant names, greetings, or filler words

Return ONLY a comma-delimited list of tags with no commentary.

Conversation (XML format, <p0> and <p1> are participants):
{{ conversation_to_analyze }}
